/// <reference types="vite/client" />

/** Build stamp injected by vite.config.ts — see the note there. */
declare const __BUILD__: string
/** versionName read straight from android/app/build.gradle — see vite.config.ts. */
declare const __APP_VERSION__: string
